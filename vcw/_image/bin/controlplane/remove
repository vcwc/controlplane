#!/usr/bin/env bash
(
  export PATH="/vcw/bin/controlplane/internal:$VWC_EXECUTIONPLANE_ORIGINALPATH"
  if [[ " stack module solution " == *"$1"* ]]; then api="$1"; shift; fi
  . cli-executor deploy $api down $@
)
