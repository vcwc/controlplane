#!/usr/bin/env bash
(
  infoFile="/vcw/assets/info-summary/$1"
  if [[ -f $infoFile ]] ; then
    $infoFile | envsubst
  else
    executionplane-warning "No info file for $1. Showing repo info."
    infoFile="/vcw/assets/info-summary/repo"
    $infoFile | envsubst
  fi
)
