#!/usr/bin/env bash
(
  infoFile="/vcwc/assets/info-summary/$1"
  if [[ -f $infoFile ]] ; then
    $infoFile | envsubst
  else
    executionplane-warning "No info file for $1. Showing repo info."
    infoFile="/vcwc/assets/info-summary/repo"
    $infoFile | envsubst
  fi
)
