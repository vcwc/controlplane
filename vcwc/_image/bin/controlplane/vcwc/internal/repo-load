#!/usr/bin/env bash
fromTar="$1"
imageName="$2"
if [[ "$fromTar" == "true" ]] ; then
  reference=$(echo "${VCWC_REGISTRY}${imageName}.tar" | tr /: _ )
  executionplane docker load --input "/vcwc/pwd/images/$reference"
else
  executionplane docker image pull "$VCWC_REGISTRY$imageName"
fi
